# Levi02114.github.io

Static personal blog for `Levi02114`, built with Next.js and exported for GitHub Pages.

Content is stored as Markdown in `content/`, the app lives in `src/`, and deployment is handled by the GitHub Pages workflow in `.github/workflows/deploy.yml`.
